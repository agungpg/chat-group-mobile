import { memo } from "react"
import { Text, View } from "react-native"

const GroupInfoScreen = () => {
  return <View>
    <Text>Group Info Screen</Text>
  </View>
}

export default memo(GroupInfoScreen)